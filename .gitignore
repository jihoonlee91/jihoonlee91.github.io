# Generated by generate.py / viz.py — rebuilt fresh on every deploy by
# .github/workflows/build.yml, so these are build output, not source.
# Run `python generate.py` locally to preview them; don't commit the result.
/index.html
/publications.html
/cv.html
/life.html
/papers/*.html
/bibtex/
/sitemap.xml
/robots.txt
