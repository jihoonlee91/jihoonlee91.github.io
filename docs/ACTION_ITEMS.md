# 오너 액션 아이템 (본인이 직접 처리해야 하는 것들)

이 문서는 `docs/ROADMAP.md` 중에서 **Claude Code가 대신 할 수 없고, 지훈님 본인이
정보를 주거나 직접 조치해야만 진행되는 항목**만 뽑아 정리한 것입니다. 코드/디자인
작업은 세션이 알아서 진행하고, 여기 있는 것만 지훈님 판단이 필요합니다.

체크박스는 실제로 처리되면 지우거나 `[x]`로 바꿔주세요. 이 문서 자체는 정적
사이트에 포함되지 않고 `docs/` 안에서만 관리됩니다(공개 저장소이긴 하지만 다른
문서들처럼 유지보수 메모라 상관없음).

## 콘텐츠 — 문장/정보를 알려줘야 하는 것

- [ ] **"Open to" / 현재 상태 한 줄** — 지금 뭘 하고 있고 뭐에 열려 있는지
  (예: "이직 고려 중", "협업 제안 환영", "구직 중 아님" 등). 리크루터와 협업
  제안자 모두가 가장 먼저 찾는 정보인데 지금은 없음. 시기별로 바뀔 수 있으니
  "지금 기준"으로 한 줄만 주면 반영.
- [ ] **"왜 GNC/UAV 제어 경험이 반도체 AI로 이어지는지" 한 문장** — 지금은
  암시만 되어 있고 명시적으로 안 써 있음. 제어이론 모르는 리크루터는 스스로
  연결 못 지음. "제어이론 → 반도체 공정 최적화/AI"를 지훈님 언어로 한두
  문장 주면 Home 페이지에 콜아웃으로 추가.
- [ ] **정량적 성과 수치** — "OO를 X% 개선" 같은 구체적 숫자. 이력서
  자료에 "20+ 인원 프로그램", "30+ 요청 지원", "10+ 데이터 소스 통합" 같은
  수치가 있는 걸 확인했지만, 전부 회사 내부 조직/규모 정보라 `docs/CONTENT_POLICY.md`
  기준상 그대로 못 씀. 공개 가능한 형태(회사명/팀 규모 없이)로 재작성해도
  괜찮은 수치가 있으면 알려주면 반영. 없으면 스킵.
- [ ] **협업 제안 문구(CTA)** — "OO 분야 협업 문의는 이메일로" 같은 명시적 문장.
  Open to 라인과 겹쳐도 됨.

## 콘텐츠 — 자료를 직접 올려줘야 하는 것

- [ ] **논문 8편, 링크 못 찾음** (2015~2018 국내 학회 발표, DBpia/RISS 두 번 조사
  다 실패) — PDF 사본 있으면 `papers/pdfs/`에 올려서 알려주면 셀프 호스팅
  링크로 연결. 사본이 없으면 이 8편은 "Coming Soon" 배지로 그대로 둠.
- [ ] **논문 25편, abstract 없음** — 같은 이유. 원문 초록 텍스트가 있으면 붙여주면
  바로 반영.
- [ ] **Side Projects** — RaceHub을 한 번 추가했다가 지훈님 요청으로 되돌림
  (배포 전이라 판단 유보한 것으로 이해). 사이트에 올리고 싶은 사이드
  프로젝트가 있으면 이름/URL/설명/스택/기간만 알려주면 다시 추가.
- [ ] **특허** — 독립적으로 공개된 특허 기록이 있다면 알려주면 추가 (내부 프로젝트
  맥락 없이, 공개된 특허 정보만).
- [ ] **발표/강연 이력** — 학회 발표, 2016년 대상 수상 발표, 사내 비기밀 기술
  발표 등.

## 액션 — 다른 플랫폼에서 지훈님이 직접 해야 하는 것

- [ ] **Google Scholar 프로필의 "Homepage" 필드**를 이 사이트 URL로 설정.
- [ ] **ResearchGate 프로필**에도 이 사이트 링크를 홈페이지로 등록.
- [ ] **지도교수 연구실(FDCL) 페이지** 등 편집 권한이 있는 곳이 있다면 이 사이트로
  백링크. (검색엔진 발견성에 실질적으로 도움됨.)

- [ ] **모핑 UAV 관련 개인 GitHub 레포 공개 여부** — 이번 조사에서
  `gtm-lpv-control`, `gtm-morphing-sim`, `morphing-hinf-control-learning`,
  `morphing-dl-gain-scheduler`, `argos-vtol-model`, `morphing-flight-control-doc`
  등 논문 주제와 관련된 레포가 이미 GitHub에 공개돼 있는 걸 발견함. 다만
  README에 특정 논문을 명시적으로 언급하진 않아서 자동으로 논문에 링크
  걸지 않음. 특정 논문과 연결하고 싶은 레포가 있으면 알려주면 그 논문의
  `code` 링크로 추가하거나, 아니면 Side Projects에 별도로 추가.

## 판단이 필요한 것 (예/아니오만 알려주면 됨)

- [ ] **커스텀 도메인** 구매 의향 있는지. 있으면 리포 구조가 바뀌어야 해서
  (GitHub Pages 유저 사이트는 리포 이름이 `<username>.github.io`여야 함)
  미리 알려주면 좋음.
- [ ] **한국어 버전(/ko/)** 필요 여부 — 지금은 영어 전용. 한국어 리크루터/방문자
  비중이 커지면 고려, 유지보수 부담 2배라 먼저 필요성 확인 필요.

---

기술적으로 코드만으로 해결 가능한 항목(프린트 스타일시트, arXiv/코드 링크 조사,
apple-touch-icon 등)은 이 문서에 넣지 않았습니다 — `docs/ROADMAP.md`의
"Technical/SEO gaps" 절을 보면 진행 상황이 있습니다.
